/* File Theme.tsx
 * Contains Mantine Theme information to be used site-wide
 */

import {createTheme, DEFAULT_THEME} from '@mantine/core'

/* Create the theme */
export const careerWiseTheme = createTheme({
	/* Primary color
	 * For now is one of the Mantine default theme colors, but custom colors
	 * could also be defined.
	 * This is currently used for the header and buttons.
	 */
	primaryColor: "yellow"
}) 